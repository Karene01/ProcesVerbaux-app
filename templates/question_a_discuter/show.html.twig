{% extends 'base.html.twig' %}

{% block title %}QuestionADiscuter{% endblock %}

{% block body %}
    <h1>QuestionADiscuter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question_a_discuter.id }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ question_a_discuter.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_question_a_discuter_index') }}">back to list</a>

    <a href="{{ path('app_question_a_discuter_edit', {'id': question_a_discuter.id}) }}">edit</a>

    {{ include('question_a_discuter/_delete_form.html.twig') }}
{% endblock %}
