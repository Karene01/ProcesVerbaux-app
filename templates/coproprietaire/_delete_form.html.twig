<form method="post" action="{{ path('app_coproprietaire_delete', {'id': coproprietaire.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coproprietaire.id) }}">
    <button class="btn">Supprimer</button>
</form>
