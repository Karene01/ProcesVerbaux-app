<form method="post" action="{{ path('app_coproprietaire_delete', {'id': coproprietaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coproprietaire.id) }}">
    <button class="btn">Delete</button>
</form>
