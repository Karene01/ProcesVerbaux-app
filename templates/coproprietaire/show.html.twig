{% extends 'base.html.twig' %}

{% block title %}Coproprietaire{% endblock %}

{% block body %}
    <h1>Coproprietaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ coproprietaire.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ coproprietaire.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ coproprietaire.prenom }}</td>
            </tr>
            <tr>
                <th>Domicile</th>
                <td>{{ coproprietaire.domicile }}</td>
            </tr>
            <tr>
                <th>QuotePart</th>
                <td>{{ coproprietaire.quotePart }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_coproprietaire_index') }}">back to list</a>

    <a href="{{ path('app_coproprietaire_edit', {'id': coproprietaire.id}) }}">edit</a>

    {{ include('coproprietaire/_delete_form.html.twig') }}
{% endblock %}
