<form method="post" action="{{ path('app_assemblee_generale_delete', {'id': assemblee_generale.id}) }}" onsubmit="return confirm('Etes-vous sûr de supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ assemblee_generale.id) }}">
    <button class="btn">Supprimer</button>
</form>
