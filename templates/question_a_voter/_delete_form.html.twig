<form method="post" action="{{ path('app_question_a_voter_delete', {'id': question_a_voter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ question_a_voter.id) }}">
    <button class="btn">Supprimer</button>
</form>
